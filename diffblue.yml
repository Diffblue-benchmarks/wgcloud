buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
#Target TGF: 64.0
# - methods: 9
# - complexity: 159
# - construction: 7.0
# - dependency: 6.9
  - target:
    - dir: target/classes
      file: com/service/host/IntrusionInfoService.class
      sourceDir: src/main/java
      sourceFile: com/service/host/IntrusionInfoService.java
#Target TGF: 297.25
# - methods: 11
# - complexity: 1593
# - construction: 11.0
# - dependency: 2.66666666667
  - target:
    - dir: target/classes
      file: com/service/dash/DashboardService.class
      sourceDir: src/main/java
      sourceFile: com/service/dash/DashboardService.java
#Target TGF: 218.05
# - methods: 13
# - complexity: 643
# - construction: 11.0
# - dependency: 5.28333333333
  - target:
    - dir: target/classes
      file: com/util/CodeUtil.class
      sourceDir: src/main/java
      sourceFile: com/util/CodeUtil.java
#Target TGF: 72.0833333333
# - methods: 18
# - complexity: 159
# - construction: 8.0
# - dependency: 6.08333333333
  - target:
    - dir: target/classes
      file: com/common/Page.class
      sourceDir: src/main/java
      sourceFile: com/common/Page.java
#Target TGF: 84.1055555556
# - methods: 11
# - complexity: 1165
# - construction: 4.33333333333
# - dependency: 2.17619047619
  - target:
    - dir: target/classes
      file: com/util/linux/LinuxStrUtil.class
      sourceDir: src/main/java
      sourceFile: com/util/linux/LinuxStrUtil.java
#Target TGF: 86.6666666667
# - methods: 14
# - complexity: 210
# - construction: 13.0
# - dependency: 8.11666666667
  - target:
    - dir: target/classes
      file: com/util/DateUtil.class
      sourceDir: src/main/java
      sourceFile: com/util/DateUtil.java
#Target TGF: 57.0
# - methods: 7
# - complexity: 132
# - construction: 4.66666666667
# - dependency: 4.2
  - target:
    - dir: target/classes
      file: com/service/msg/MsgInfoService.class
      sourceDir: src/main/java
      sourceFile: com/service/msg/MsgInfoService.java
#Target TGF: 107.166666667
# - methods: 22
# - complexity: 234
# - construction: 21.0
# - dependency: 12.3166666667
  - target:
    - dir: target/classes
      file: com/util/Encode.class
      sourceDir: src/main/java
      sourceFile: com/util/Encode.java
#Target TGF: 55.0
# - methods: 8
# - complexity: 114
# - construction: 6.0
# - dependency: 6.7
  - target:
    - dir: target/classes
      file: com/service/host/SystemInfoService.class
      sourceDir: src/main/java
      sourceFile: com/service/host/SystemInfoService.java
#Target TGF: 174.19047619
# - methods: 14
# - complexity: 451
# - construction: 14.0
# - dependency: 6.55952380952
  - target:
    - dir: target/classes
      file: com/util/FormatUtil.class
      sourceDir: src/main/java
      sourceFile: com/util/FormatUtil.java
dependenciesOnClasspath:
-
  source: classes
  classFile: com/util/FormatUtil$DoubleComparator.class
-
  source: classes
  classFile: com/util/UUIDUtil.class
-
  source: classes
  classFile: com/entity/app/AppState.class
-
  source: classes
  classFile: com/entity/host/CpuState.class
-
  source: classes
  classFile: com/dao/host/SystemInfoDao.class
-
  source: classes
  classFile: com/dao/msg/MsgInfoDao.class
-
  source: classes
  classFile: com/entity/host/SystemInfo.class
-
  source: classes
  classFile: com/util/CodeUtil.class
-
  source: classes
  classFile: com/common/Page.class
-
  source: classes
  classFile: com/entity/host/IntrusionInfo.class
-
  source: classes
  classFile: com/util/Encode.class
-
  source: classes
  classFile: com/entity/msg/MsgInfo.class
-
  source: classes
  classFile: com/util/FormatUtil.class
-
  source: classes
  classFile: com/entity/host/DiskIoState.class
-
  source: classes
  classFile: com/entity/host/SysLoadState.class
-
  source: classes
  classFile: com/entity/host/DeskState.class
-
  source: classes
  classFile: com/entity/host/MemState.class
-
  source: classes
  classFile: com/entity/host/NetIoState.class
-
  source: classes
  classFile: com/dao/host/IntrusionInfoDao.class
-
  source: classes
  classFile: com/entity/host/TcpState.class
-
  source: classes
  classFile: com/util/DateUtil.class